import ui from './ui'
import globalConfig from './global_data'
import reg from './formReg'
import cache from './cache'

const utils = {
   ui,
   reg,
   cache,
   globalConfig
}

export default utils